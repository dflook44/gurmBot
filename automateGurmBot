import gurmBot
from datetime import datetime
from threading import Timer

x=datetime.today()
y=x.replace(day=x.day, hour=15, minute=30, second=0, microsecond=0)
delta_t=y-x

secs=delta_t.seconds+1

t = Timer(secs, gurmBot.main('http://grrm.livejournal.com/'))
t.start()
